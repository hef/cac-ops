echo "
menuentry 'netboot' {
    linux16 (lvm/ubuntu--vg-root)/boot/netboot.xyz.lkrn set net0/ip {{ ansible_host }} && set net0/netmask {{cac_netmask}} && set net0/gateway {{cac_gateway}} && set dns 8.8.8.8 && ifopen net0 && chain --autofree https://boot.netboot.xyz
}
"
